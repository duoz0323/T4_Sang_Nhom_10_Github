package stu.edu.Backend_Nhom10.enums;

public enum Status {
    ACCEPTED,
    ACTIVE, //đã hiển thị đã được duyệt
    PENDING, //chờ duyệt
    REJECTED, //bị từ chối
    CLOSED, //đã đóng
    EXPIRED, //hết hạn
}
