spring:
  datasource:
    url: jdbc:mysql://localhost:3306/quanlytuyendung_nhom10
    username: root
    password:
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

  # 3. Cấu hình Keycloak (Xác minh Token)
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: https://lemur-10.cloud-iam.com/auth/realms/webtuyendung-nhom10
idp:
  url: https://lemur-10.cloud-iam.com/auth
  client-id: webtuyendung_id
  client-secret: ANvD8KzGm1CLfXCPJyxI9yVseVUHr2i3
  realm: webtuyendung-nhom10
cloudinary:
  cloud-name: ${CLOUDINARY_CLOUD_NAME:test}
  api-key: ${CLOUDINARY_CLOUD_NAME:test}
  api-secret: ${CLOUDINARY_API_KEY:test}
